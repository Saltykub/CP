#include<bits/stdc++.h>
#define ll long long 
#define st first
#define pii pair<ll,ll>
#define nd second
#define pb push_back
using namespace std;
const int N = 2e6+10;
bool CASE = false;
vector<int> p(N);

void solve (){
    int n,k;
    cin >> n >> k;
    for(int i = 0; i < k; i++){
        int x;
        cin >> x;

    }
}
int main(){   
    ios_base::sync_with_stdio(false);cin.tie(0);
    int t = 1;
    if(CASE) cin >> t;
    while(t--){   
        solve();
    }
}